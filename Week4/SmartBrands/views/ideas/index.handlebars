{{#each ideas}}
    <div class="card card-body mb-2">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <a href="/ideas/edit/{{id}}" class="btn btn-dark block">Edit</a>
    </div>
{{else}}
    <p>No Ideas were found!((</p>
{{/each}}